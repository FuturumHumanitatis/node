<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Смотрилка - Твоя библиотека</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background: #071028;
      font-family: "Segoe UI", Roboto, Arial, sans-serif;
      color: #ffffff;
      margin: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .topbar {
      background: rgba(255, 255, 255, 0.02);
      padding: 0.75rem 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .topbar .brand {
      font-weight: 800;
      font-size: 1.5rem;
    }
    .topbar .user-actions a {
      color: #ffffff;
      margin-left: 1rem;
      text-decoration: none;
    }
    .topbar .user-actions a:hover {
      text-decoration: underline;
    }
    .content {
      padding: 1rem;
      flex: 1;
    }
    .movie-card {
      background: rgba(255, 255, 255, 0.03);
      border-radius: 8px;
      margin-bottom: 1rem;
      display: flex;
      padding: 1rem;
      align-items: flex-start;
    }
    .movie-card img {
      width: 100px;
      height: 150px;
      object-fit: cover;
      margin-right: 1rem;
      border-radius: 4px;
    }
    .movie-details {
      flex: 1;
    }
    .movie-details a {
      color: #ffffff;
      text-decoration: none;
    }
    .movie-details a:hover {
      text-decoration: underline;
    }
    .movie-meta {
      color: #9fb0d4;
      font-size: 0.9rem;
      margin-top: 0.25rem;
    }
    .add-movie-btn {
      background: #1bd760;
      color: white;
      text-decoration: none;
      display: inline-block;
      border-radius: 4px;
      padding: 0.5rem 1rem;
    }
    .add-movie-btn:hover {
      background: #1db954;
    }
  </style>
</head>
<body>
  <header class="topbar">
    <div class="brand">Смотрилка</div>
    <div class="user-actions">
      <% if (currentUser) { %>
        <span>Привет, <%= currentUser.username %>!</span>
        <a href="/logout">Выйти</a>
      <% } else { %>
        <a href="/login">Войти</a>
        <a href="/register">Регистрация</a>
      <% } %>
    </div>
  </header>

  <main class="content">
    <h1>Все фильмы</h1>

    <% movies.forEach(movie => { %>
      <div class="movie-card">
        <img src="<%= movie.poster_url || 'https://via.placeholder.com/100x150.png?text=No+Poster' %>" alt="<%= movie.title %>">
        <div class="movie-details">
          <a href="/movie/<%= movie.id %>">
            <h3><%= movie.title %></h3>
          </a>
          <div class="movie-meta">
            <span>Рейтинг: <%= movie.rating %>/10</span> |
            <span>Добавил: <%= movie.added_by %></span>
          </div>
        </div>
      </div>
    <% }) %>

    <% if (currentUser) { %>
      <a href="/add" class="add-movie-btn">Добавить фильм</a>
    <% } %>
  </main>
</body>
</html>